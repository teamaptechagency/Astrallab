node_modules/
.next/
.env
*.db
*.db-journal
next-env.d.ts
tsconfig.tsbuildinfo
packages/
.next-build/

# Uploaded release artefacts. Real ones are large; seeded placeholders are
# regenerated by `npm run db:seed`.
packages/*/
astralab-website.zip

# The Laravel app's own ignores are in site/.gitignore; this keeps the built
# dependency tree out of the parent repo's index.
site/vendor/
site/node_modules/
site/astralab-site.zip
